# Laravel CRM システム要件定義書

## 1. プロジェクト概要

### 1.1 システム名
Laravel CRM システム

### 1.2 開発目的
顧客関係管理（CRM）機能を提供し、企業の営業活動を効率化する

### 1.3 対象システム
- Laravel 12.0 + Livewire/Flux フレームワークを使用したWebアプリケーション
- SQLiteデータベースを使用

## 2. システム概要

### 2.1 システムの特徴
- Laravel Livewire Starter Kitをベースとした高性能CRMシステム
- Tailwind CSSによるモダンなUI/UX
- 認証機能を標準搭載
- Pestによる包括的なテスト環境

### 2.2 主な機能
- ユーザー管理・認証
- 会社情報管理
- ダッシュボード機能
- 設定管理

## 3. 機能要件

### 3.1 認証機能
#### 3.1.1 ユーザー登録
- 名前、メールアドレス、パスワードによる登録
- メール認証機能

#### 3.1.2 ログイン・ログアウト
- メールアドレス・パスワードによる認証
- Remember me機能
- パスワードリセット機能

#### 3.1.3 プロフィール管理
- ユーザー情報の更新
- パスワード変更
- アカウント削除

### 3.2 会社管理機能
#### 3.2.1 会社情報登録
- 必須項目：会社名
- 任意項目：
  - メールアドレス
  - 電話番号
  - Webサイト
  - 住所
  - 郵便番号
  - 市区町村
  - 業界
  - 説明
  - 備考

#### 3.2.2 会社一覧表示
- 登録済み会社の一覧表示
- ID、会社名の表示
- アクション機能（詳細・編集・削除）

### 3.3 ダッシュボード機能
- ユーザー専用のダッシュボード表示
- システム概要の表示

### 3.4 設定機能
- プロフィール設定
- パスワード変更
- 外観設定（テーマ変更）

## 4. 非機能要件

### 4.1 性能要件
- ページ読み込み時間：3秒以内
- 同時接続ユーザー数：100ユーザー以上

### 4.2 セキュリティ要件
- Laravel標準の認証機能を使用
- CSRFプロテクション
- SQLインジェクション対策
- パスワードハッシュ化

### 4.3 可用性要件
- システム稼働率：99%以上
- データバックアップ機能

### 4.4 保守性要件
- Pestによる自動テスト
- Laravel Pintによるコード品質管理

## 5. 技術仕様

### 5.1 開発環境
- **フレームワーク**: Laravel 12.0
- **言語**: PHP 8.2以上
- **フロントエンド**: Livewire + Flux UI
- **CSS**: Tailwind CSS 4.0
- **データベース**: SQLite
- **テスト**: Pest
- **ビルドツール**: Vite

### 5.2 主要パッケージ
- livewire/flux: UI コンポーネント
- livewire/volt: Livewire拡張
- pestphp/pest: テストフレームワーク
- laravel/pint: コード整形

## 6. データベース設計

### 6.1 テーブル構成

#### users テーブル
| カラム名 | データ型 | 制約 | 説明 |
|---------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | ユーザーID |
| name | VARCHAR(255) | NOT NULL | ユーザー名 |
| email | VARCHAR(255) | UNIQUE, NOT NULL | メールアドレス |
| email_verified_at | TIMESTAMP | NULL | メール認証日時 |
| password | VARCHAR(255) | NOT NULL | パスワード（ハッシュ化） |
| remember_token | VARCHAR(100) | NULL | Remember meトークン |
| created_at | TIMESTAMP | NULL | 作成日時 |
| updated_at | TIMESTAMP | NULL | 更新日時 |

#### companies テーブル
| カラム名 | データ型 | 制約 | 説明 |
|---------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 会社ID |
| name | VARCHAR(255) | NOT NULL | 会社名 |
| email | VARCHAR(255) | NULL | メールアドレス |
| phone | VARCHAR(255) | NULL | 電話番号 |
| website | VARCHAR(255) | NULL | Webサイト |
| address | TEXT | NULL | 住所 |
| postal_code | VARCHAR(255) | NULL | 郵便番号 |
| city | VARCHAR(255) | NULL | 市区町村 |
| industry | VARCHAR(255) | NULL | 業界 |
| description | TEXT | NULL | 説明 |
| notes | TEXT | NULL | 備考 |
| created_at | TIMESTAMP | NULL | 作成日時 |
| updated_at | TIMESTAMP | NULL | 更新日時 |

#### その他システムテーブル
- password_reset_tokens: パスワードリセット用
- sessions: セッション管理用
- cache: キャッシュ用
- jobs: ジョブキュー用

## 7. 画面設計

### 7.1 認証関連画面
- ログイン画面 (`/login`)
- ユーザー登録画面 (`/register`)
- パスワードリセット画面 (`/forgot-password`)
- メール認証画面 (`/verify-email`)

### 7.2 メイン機能画面
- ダッシュボード (`/dashboard`)
- 会社一覧画面 (`/dashboard/companies`)

### 7.3 設定画面
- プロフィール設定 (`/settings/profile`)
- パスワード設定 (`/settings/password`)
- 外観設定 (`/settings/appearance`)

## 8. ルーティング設計

### 8.1 認証ルート
- Laravel Breezeスターターキットの標準認証ルートを使用

### 8.2 アプリケーションルート
```php
// ダッシュボード
Route::view('dashboard', 'dashboard')->middleware(['auth', 'verified'])->name('dashboard');

// 会社管理
Route::get('dashboard/companies', [CompanyController::class, 'index'])->middleware(['auth'])->name('companies.index');

// 設定（Voltルート）
Route::middleware(['auth'])->group(function () {
    Volt::route('settings/profile', 'settings.profile')->name('settings.profile');
    Volt::route('settings/password', 'settings.password')->name('settings.password');
    Volt::route('settings/appearance', 'settings.appearance')->name('settings.appearance');
});
```

## 9. 開発スケジュール

### Phase 1: 基盤開発（完了）
- Laravel環境構築
- 認証機能実装
- 基本的なUI/UX構築

### Phase 2: 会社管理機能拡張
- 会社情報の詳細表示
- 会社情報の編集機能
- 会社情報の削除機能
- 検索・フィルター機能

### Phase 3: 追加機能
- 顧客（個人）管理機能
- 案件管理機能
- 活動履歴管理

### Phase 4: 最適化・テスト
- パフォーマンス最適化
- セキュリティ監査
- 包括的テスト実行

## 10. 保守・運用

### 10.1 ログ管理
- Laravel標準ログ機能を使用
- エラーログ、アクセスログの記録

### 10.2 バックアップ
- データベースの定期バックアップ
- アプリケーションファイルのバックアップ

### 10.3 監視
- アプリケーションの正常性監視
- パフォーマンス監視

## 11. 今後の拡張予定

### 11.1 機能拡張
- API機能（REST API）
- モバイルアプリ対応
- 外部システム連携（メール配信、会計システム等）

### 11.2 技術的拡張
- PostgreSQL/MySQLへの移行対応
- Redis キャッシュ対応
- Elasticsearch 検索機能

---

**作成日**: 2025年8月21日  
**バージョン**: 1.0  
**承認者**: [承認者名を記入]